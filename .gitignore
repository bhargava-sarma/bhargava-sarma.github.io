# Local tool/editor state — never belongs in the published site
.claude/settings.local.json
.vscode/
.idea/

# OS cruft
.DS_Store
Thumbs.db

# Local dev artifacts
*.log

# Python bytecode — scripts/dashboard.py imports build.py, so running the
# dashboard generates these
__pycache__/
*.py[cod]
